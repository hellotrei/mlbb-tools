FROM node:20-alpine

WORKDIR /app

COPY . .

RUN corepack enable \
  && pnpm install --frozen-lockfile

ENV NODE_ENV=production
EXPOSE 8787

CMD ["pnpm", "--filter", "@mlbb/api", "exec", "tsx", "src/index.ts"]
