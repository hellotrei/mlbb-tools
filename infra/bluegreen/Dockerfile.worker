FROM node:20-alpine

WORKDIR /app

COPY . .

RUN corepack enable \
  && pnpm install --frozen-lockfile

ENV NODE_ENV=production

CMD ["pnpm", "--filter", "@mlbb/worker", "exec", "tsx", "src/index.ts"]
